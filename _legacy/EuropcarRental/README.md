# Europcar Rental (monolito)

Solución: `Europcar.Rental.slnx`

```bash
dotnet restore
dotnet run --project src/Europcar.Rental.Api
```

API por defecto (según `launchSettings` del proyecto): revisar `Properties/launchSettings.json` del API.
