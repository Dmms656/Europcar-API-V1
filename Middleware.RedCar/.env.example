# =====================================================
# Middleware.RedCar - Variables de entorno
# =====================================================
# Copia este archivo a Middleware.RedCar/.env y rellena los valores.
# El .env real esta cubierto por el patron ".env" del .gitignore del repo raiz.
# =====================================================

# --- Base de datos (esquema security; mismo Postgres que uses para seed seguridad) ---
# En Render: ConnectionStrings__Default con la cadena Npgsql completa (Supabase pooler 6543).
# El middleware fuerza Multiplexing=false, NoResetOnClose=true y MaxAutoPrepare=0 si el Host
# contiene pooler.supabase.com (evita "Exception while reading from stream").
# Ejemplo (sustituye host, usuario ms_seguridad.<PROJECT_REF> y password):
# ConnectionStrings__Default=Host=aws-0-REGION.pooler.supabase.com;Port=6543;Database=postgres;Username=ms_seguridad.xxxxx;Password=...;SSL Mode=Require;Trust Server Certificate=true;Pooling=true;Maximum Pool Size=10;Include Error Detail=true;
ConnectionStrings__Default=

# Swagger UI en Production (Render): true para abrir https://tu-api/swagger
Swagger__Enabled=false

# --- URLs de microservicios (HTTP; Auth va embebido en el middleware, no hace falta MS Seguridad) ---
Microservicios__Catalogo__BaseUrl=http://localhost:5102
Microservicios__Localizaciones__BaseUrl=http://localhost:5103
Microservicios__Clientes__BaseUrl=http://localhost:5104
Microservicios__Reservas__BaseUrl=http://localhost:5105

# Timeouts por cliente HTTP (segundos)
Microservicios__Catalogo__TimeoutSeconds=60
Microservicios__Localizaciones__TimeoutSeconds=60
Microservicios__Clientes__TimeoutSeconds=120
Microservicios__Reservas__TimeoutSeconds=90

# --- JWT (emisión de tokens en el mismo proceso; mismo secreto que valida el middleware) ---
Jwt__SecretKey=redcar-v2-dev-jwt-secret-cambiar-en-prod-min-32b
Jwt__Issuer=redcar-v2
Jwt__Audience=redcar-v2-clients
Jwt__ExpirationMinutes=60

# --- IVA aplicado a las reservas (porcentaje) ---
Negocio__IvaPorcentaje=15
